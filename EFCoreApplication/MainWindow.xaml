<Window x:Class="EFCoreApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EFCoreApplication"
        xmlns:viewModels="clr-namespace:EFCoreApplication.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewModels:MainViewModel}"
        Title="MainWindow" Height="450" Width="800">

  <Window.Resources>
    <viewModels:MainViewModel x:Key="MainViewModelDataContext" />
  </Window.Resources>

  <Grid DataContext="{StaticResource MainViewModelDataContext}">
    <Grid.RowDefinitions>
      <RowDefinition Height="0.4*" />
      <RowDefinition Height="*" />
      <RowDefinition Height="*" />
      <RowDefinition Height="*" />
      <RowDefinition Height="10*" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="0.05*" />
      <ColumnDefinition Width="0.9*" />
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <TextBlock Text="Add new project number:" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" />
    <TextBlock Text="Select project number:" Grid.Row="2" Grid.Column="1" VerticalAlignment="Center" />


    <TextBox Grid.Row="1" Text="{Binding ProjectNumber}" Grid.Column="2" VerticalAlignment="Center"/>
    <ComboBox Grid.Row="2" Grid.Column="2" VerticalAlignment="Center" ItemsSource="{Binding ProjectNumberList}" SelectedItem="{Binding SelectedProjectNumber}" />

    <Button Content="Add tasks" Command="{Binding AddTasksToDatabaseCommand}" Grid.Row="1" Grid.Column="5" Margin="20,2,20,2"/>
    <!--<Button Content="Refresh" Grid.Row="2" Grid.Column="5" Margin="20,2,20,2"/>-->
    <Button Content="Add" Command="{Binding AddProjectToDatabaseCommand}" Grid.Row="1" Grid.Column="3" Margin="20,2,20,2"/>

    <DataGrid x:Name="DataGriddd" Grid.Row="4" Grid.ColumnSpan="6" ItemsSource="{Binding ProjectTable}"/>

  </Grid>
</Window>
