<Window x:Class="EFCoreApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EFCoreApplication"
        xmlns:viewModels="clr-namespace:EFCoreApplication.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewModels:MainViewModel}"
        Title="MainWindow" Height="450" Width="800">
  
    <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*" />
      <RowDefinition Height="*" />
      <RowDefinition Height="*" />
      <RowDefinition Height="*" />
      <RowDefinition Height="*" />
      <RowDefinition Height="10*" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="0.5*" />
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <TextBlock Text="Project number:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" />
    <TextBlock Text="Project number:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" />
    <TextBlock Text="Project number:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" />
    <TextBlock Text="Project number:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" />

    <Button Content="Remove" Grid.Row="0" Grid.Column="3" Margin="20,2,20,2"/>
    <Button Content="Refresh" Grid.Row="1" Grid.Column="3" Margin="20,2,20,2"/>

    <DataGrid x:Name="DataGriddd" Grid.Row="5" Grid.ColumnSpan="4" ItemsSource="{Binding ProjectTable}"/>

  </Grid>
</Window>
